gamelan.registry.persistence.type=database

# Suppress Hibernate JSON format mapper warning
quarkus.hibernate-orm.mapping.format.global=ignore

# Disable Hibernate Reactive datasource validation in tests.
# Persistence tests use unit-level fakes instead of starting Quarkus/JPA.
quarkus.hibernate-orm.enabled=false
quarkus.datasource.health.enabled=false
