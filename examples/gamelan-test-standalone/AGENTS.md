# Gamelan Standalone Simulation Example

Implement all Gamelan module in one project/instance, with following features:

- combine all gamelan module into one project/instance
- create test workflow that use all gamelan module
